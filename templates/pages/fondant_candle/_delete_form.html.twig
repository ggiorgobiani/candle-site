<form method="post" action="{{ path('app_fondant_candle_delete', {'id': fondant_candle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fondant_candle.id) }}">
    <button class="btn btn-danger" style="width: 200px">Delete</button>
</form>

