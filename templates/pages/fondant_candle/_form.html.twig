{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning" type="reset" style="width: 200px">Cancel</button>
    <button class="btn btn-success" style="width: 200px">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
