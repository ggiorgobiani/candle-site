<form method="post" action="{{ path('app_poured_candle_delete', {'id': poured_candle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poured_candle.id) }}">
    <button class="btn btn-danger" style="width: 200px">Delete</button>
</form>
