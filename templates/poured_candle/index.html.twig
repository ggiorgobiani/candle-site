{% extends 'base.html.twig' %}

{% block title %}PouredCandle index{% endblock %}

{% block body %}
    <h1>PouredCandle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for poured_candle in poured_candles %}
            <tr>
                <td>{{ poured_candle.id }}</td>
                <td>{{ poured_candle.name }}</td>
                <td>{{ poured_candle.description }}</td>
                <td>{{ poured_candle.price }}</td>
                {# <td>{{ poured_candle.quantity }}</td> #}
                <td>
                <img src="/uploads/{{ poured_candle.image }}" style="width: 200px" alt="">
                </td>
                <td>
                    <a href="{{ path('app_poured_candle_show', {'id': poured_candle.id}) }}">show</a>
                    <a href="{{ path('app_poured_candle_edit', {'id': poured_candle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_poured_candle_new') }}">Create new</a>
{% endblock %}
